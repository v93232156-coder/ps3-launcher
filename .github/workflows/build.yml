name: Build_PS3
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/bucanero/ps3dev:latest
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: |
          make
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: My_App_PKG
          path: ./*.pkg
